[project]
name = "viewer"
version = "0.1.0"
description = "Article viewer for the NG fetcher pipeline"
requires-python = ">=3.14"
dependencies = [
    "common",
    "fastapi[standard]>=0.115",
    "sqlalchemy[asyncio]>=2.0",
    "asyncpg>=0.30",
    "python-dotenv>=1.0",
    "jinja2>=3.1",
]

[tool.uv.sources]
common = { path = "../common", editable = true }
