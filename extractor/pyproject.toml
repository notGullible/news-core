[project]
name = "extractor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "common",
    "logging>=0.4.9.6",
]

[tool.uv.sources]
common = { path = "../common", editable = true }