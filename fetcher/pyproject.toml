[project]
name = "fetcher"
version = "0.1.0"
description = "Multi-site news article fetcher, scraper, and crawler pipeline"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "botasaurus>=4.0.97",
    "redis>=7.4.0",
    "sqlalchemy[asyncio]>=2.0",
    "asyncpg>=0.30",
    "python-dotenv>=1.0",
    "alembic>=1.14",
]
